-- Generated using RoadToGlory's Converter v1.1 (RoadToGlory#9879)

-- Instances:

local Converted = {
	["_Admin Panel"] = Instance.new("ScreenGui");
	["_1"] = Instance.new("Frame");
	["_TextLabel"] = Instance.new("TextLabel");
	["_2"] = Instance.new("Frame");
	["_TextButton"] = Instance.new("TextButton");
	["_LocalScript"] = Instance.new("LocalScript");
	["_TextButton1"] = Instance.new("TextButton");
	["_LocalScript1"] = Instance.new("LocalScript");
	["_TextButton2"] = Instance.new("TextButton");
	["_LocalScript2"] = Instance.new("LocalScript");
	["_FreecamButton"] = Instance.new("TextButton");
	["_LocalScript3"] = Instance.new("LocalScript");
	["_TextButton3"] = Instance.new("TextButton");
	["_LocalScript4"] = Instance.new("LocalScript");
	["_TextButton4"] = Instance.new("TextButton");
	["_LocalScript5"] = Instance.new("LocalScript");
	["_TextButton5"] = Instance.new("TextButton");
	["_LocalScript6"] = Instance.new("LocalScript");
	["_TextButton6"] = Instance.new("TextButton");
	["_LocalScript7"] = Instance.new("LocalScript");
	["_TextButton7"] = Instance.new("TextButton");
	["_LocalScript8"] = Instance.new("LocalScript");
	["_Button"] = Instance.new("TextButton");
	["_LocalScript9"] = Instance.new("LocalScript");
	["_TextButton8"] = Instance.new("TextButton");
	["_LocalScript10"] = Instance.new("LocalScript");
	["_TextButton9"] = Instance.new("TextButton");
	["_LocalScript11"] = Instance.new("LocalScript");
	["_TextButton10"] = Instance.new("TextButton");
	["_LocalScript12"] = Instance.new("LocalScript");
	["_TextButton11"] = Instance.new("TextButton");
	["_LocalScript13"] = Instance.new("LocalScript");
	["_slide"] = Instance.new("TextButton");
	["_LocalScript14"] = Instance.new("LocalScript");
	["_UIGridLayout"] = Instance.new("UIGridLayout");
	["_TextButton12"] = Instance.new("TextButton");
	["_LocalScript15"] = Instance.new("LocalScript");
	["_TextButton13"] = Instance.new("TextButton");
	["_LocalScript16"] = Instance.new("LocalScript");
	["_TextButton14"] = Instance.new("TextButton");
	["_LocalScript17"] = Instance.new("LocalScript");
	["_MoveFrame"] = Instance.new("LocalScript");
	["_TextLabel1"] = Instance.new("TextLabel");
}

-- Properties:

Converted["_Admin Panel"].ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Converted["_Admin Panel"].Name = "Admin Panel"
Converted["_Admin Panel"].Parent = game:GetService("CoreGui")

Converted["_1"].BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Converted["_1"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_1"].BorderSizePixel = 0
Converted["_1"].Position = UDim2.new(0.292006522, 0, 0.0324999988, 0)
Converted["_1"].Size = UDim2.new(0, 509, 0, 258)
Converted["_1"].Name = "1"
Converted["_1"].Parent = Converted["_Admin Panel"]

Converted["_TextLabel"].Font = Enum.Font.Cartoon
Converted["_TextLabel"].Text = "Admin Panel Legacy Look"
Converted["_TextLabel"].TextColor3 = Color3.fromRGB(255, 255, 255)
Converted["_TextLabel"].TextScaled = true
Converted["_TextLabel"].TextSize = 14
Converted["_TextLabel"].TextWrapped = true
Converted["_TextLabel"].BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Converted["_TextLabel"].BackgroundTransparency = 1
Converted["_TextLabel"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextLabel"].BorderSizePixel = 0
Converted["_TextLabel"].Size = UDim2.new(0, 205, 0, 37)
Converted["_TextLabel"].Parent = Converted["_1"]

Converted["_2"].BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Converted["_2"].BackgroundTransparency = 1
Converted["_2"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_2"].BorderSizePixel = 0
Converted["_2"].Position = UDim2.new(0.0346945897, 0, 0.143410847, 0)
Converted["_2"].Size = UDim2.new(0, 472, 0, 221)
Converted["_2"].Name = "2"
Converted["_2"].Parent = Converted["_1"]

Converted["_TextButton"].Font = Enum.Font.Cartoon
Converted["_TextButton"].Text = "weeeee"
Converted["_TextButton"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton"].TextScaled = true
Converted["_TextButton"].TextSize = 14
Converted["_TextButton"].TextWrapped = true
Converted["_TextButton"].BackgroundColor3 = Color3.fromRGB(85.0000025331974, 170.0000050663948, 0)
Converted["_TextButton"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton"].BorderSizePixel = 0
Converted["_TextButton"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton"].Parent = Converted["_2"]

Converted["_TextButton1"].Font = Enum.Font.Cartoon
Converted["_TextButton1"].Text = "fly"
Converted["_TextButton1"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton1"].TextScaled = true
Converted["_TextButton1"].TextSize = 14
Converted["_TextButton1"].TextWrapped = true
Converted["_TextButton1"].BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Converted["_TextButton1"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton1"].BorderSizePixel = 0
Converted["_TextButton1"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton1"].Parent = Converted["_2"]

Converted["_TextButton2"].Font = Enum.Font.Cartoon
Converted["_TextButton2"].Text = "Noclip"
Converted["_TextButton2"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton2"].TextScaled = true
Converted["_TextButton2"].TextSize = 14
Converted["_TextButton2"].TextWrapped = true
Converted["_TextButton2"].BackgroundColor3 = Color3.fromRGB(0, 0, 255)
Converted["_TextButton2"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton2"].BorderSizePixel = 0
Converted["_TextButton2"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton2"].Parent = Converted["_2"]

Converted["_FreecamButton"].Font = Enum.Font.Cartoon
Converted["_FreecamButton"].Text = "Freecam"
Converted["_FreecamButton"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_FreecamButton"].TextScaled = true
Converted["_FreecamButton"].TextSize = 14
Converted["_FreecamButton"].TextWrapped = true
Converted["_FreecamButton"].BackgroundColor3 = Color3.fromRGB(255, 255, 0)
Converted["_FreecamButton"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_FreecamButton"].BorderSizePixel = 0
Converted["_FreecamButton"].Size = UDim2.new(0, 48, 0, 22)
Converted["_FreecamButton"].Name = "FreecamButton"
Converted["_FreecamButton"].Parent = Converted["_2"]

Converted["_TextButton3"].Font = Enum.Font.Cartoon
Converted["_TextButton3"].Text = "Click TP"
Converted["_TextButton3"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton3"].TextScaled = true
Converted["_TextButton3"].TextSize = 14
Converted["_TextButton3"].TextWrapped = true
Converted["_TextButton3"].BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Converted["_TextButton3"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton3"].BorderSizePixel = 0
Converted["_TextButton3"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton3"].Parent = Converted["_2"]

Converted["_TextButton4"].Font = Enum.Font.Cartoon
Converted["_TextButton4"].Text = "Extra Jump"
Converted["_TextButton4"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton4"].TextScaled = true
Converted["_TextButton4"].TextSize = 14
Converted["_TextButton4"].TextWrapped = true
Converted["_TextButton4"].BackgroundColor3 = Color3.fromRGB(85.0000025331974, 170.0000050663948, 0)
Converted["_TextButton4"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton4"].BorderSizePixel = 0
Converted["_TextButton4"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton4"].Parent = Converted["_2"]

Converted["_TextButton5"].Font = Enum.Font.Cartoon
Converted["_TextButton5"].Text = "FAST"
Converted["_TextButton5"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton5"].TextScaled = true
Converted["_TextButton5"].TextSize = 14
Converted["_TextButton5"].TextWrapped = true
Converted["_TextButton5"].BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Converted["_TextButton5"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton5"].BorderSizePixel = 0
Converted["_TextButton5"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton5"].Parent = Converted["_2"]

Converted["_TextButton6"].Font = Enum.Font.Cartoon
Converted["_TextButton6"].Text = "Fling"
Converted["_TextButton6"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton6"].TextScaled = true
Converted["_TextButton6"].TextSize = 14
Converted["_TextButton6"].TextWrapped = true
Converted["_TextButton6"].BackgroundColor3 = Color3.fromRGB(0, 0, 255)
Converted["_TextButton6"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton6"].BorderSizePixel = 0
Converted["_TextButton6"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton6"].Parent = Converted["_2"]

Converted["_TextButton7"].Font = Enum.Font.Cartoon
Converted["_TextButton7"].Text = "Inf Jump"
Converted["_TextButton7"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton7"].TextScaled = true
Converted["_TextButton7"].TextSize = 14
Converted["_TextButton7"].TextWrapped = true
Converted["_TextButton7"].BackgroundColor3 = Color3.fromRGB(255, 255, 0)
Converted["_TextButton7"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton7"].BorderSizePixel = 0
Converted["_TextButton7"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton7"].Parent = Converted["_2"]

Converted["_Button"].Font = Enum.Font.Cartoon
Converted["_Button"].Text = "wtf"
Converted["_Button"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_Button"].TextScaled = true
Converted["_Button"].TextSize = 14
Converted["_Button"].TextWrapped = true
Converted["_Button"].BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Converted["_Button"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_Button"].BorderSizePixel = 0
Converted["_Button"].Size = UDim2.new(0, 48, 0, 22)
Converted["_Button"].Name = "Button"
Converted["_Button"].Parent = Converted["_2"]

Converted["_TextButton8"].Font = Enum.Font.Cartoon
Converted["_TextButton8"].Text = "wheel"
Converted["_TextButton8"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton8"].TextScaled = true
Converted["_TextButton8"].TextSize = 14
Converted["_TextButton8"].TextWrapped = true
Converted["_TextButton8"].BackgroundColor3 = Color3.fromRGB(85.0000025331974, 170.0000050663948, 0)
Converted["_TextButton8"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton8"].BorderSizePixel = 0
Converted["_TextButton8"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton8"].Parent = Converted["_2"]

Converted["_TextButton9"].Font = Enum.Font.Cartoon
Converted["_TextButton9"].Text = "freeze"
Converted["_TextButton9"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton9"].TextScaled = true
Converted["_TextButton9"].TextSize = 14
Converted["_TextButton9"].TextWrapped = true
Converted["_TextButton9"].BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Converted["_TextButton9"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton9"].BorderSizePixel = 0
Converted["_TextButton9"].Position = UDim2.new(0.224576265, 0, 0.122171946, 0)
Converted["_TextButton9"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton9"].Parent = Converted["_2"]

Converted["_TextButton10"].Font = Enum.Font.Cartoon
Converted["_TextButton10"].Text = "inf health"
Converted["_TextButton10"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton10"].TextScaled = true
Converted["_TextButton10"].TextSize = 14
Converted["_TextButton10"].TextWrapped = true
Converted["_TextButton10"].BackgroundColor3 = Color3.fromRGB(0, 0, 255)
Converted["_TextButton10"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton10"].BorderSizePixel = 0
Converted["_TextButton10"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton10"].Parent = Converted["_2"]

Converted["_TextButton11"].Font = Enum.Font.Cartoon
Converted["_TextButton11"].Text = "TPTON"
Converted["_TextButton11"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton11"].TextScaled = true
Converted["_TextButton11"].TextSize = 14
Converted["_TextButton11"].TextWrapped = true
Converted["_TextButton11"].BackgroundColor3 = Color3.fromRGB(255, 255, 0)
Converted["_TextButton11"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton11"].BorderSizePixel = 0
Converted["_TextButton11"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton11"].Parent = Converted["_2"]

Converted["_slide"].Font = Enum.Font.Cartoon
Converted["_slide"].Text = "slide"
Converted["_slide"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_slide"].TextScaled = true
Converted["_slide"].TextSize = 14
Converted["_slide"].TextWrapped = true
Converted["_slide"].BackgroundColor3 = Color3.fromRGB(85.0000025331974, 170.0000050663948, 0)
Converted["_slide"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_slide"].BorderSizePixel = 0
Converted["_slide"].Size = UDim2.new(0, 48, 0, 22)
Converted["_slide"].Name = "slide"
Converted["_slide"].Parent = Converted["_2"]

Converted["_UIGridLayout"].CellSize = UDim2.new(0, 48, 0, 22)
Converted["_UIGridLayout"].SortOrder = Enum.SortOrder.LayoutOrder
Converted["_UIGridLayout"].Parent = Converted["_2"]

Converted["_TextButton12"].Font = Enum.Font.Cartoon
Converted["_TextButton12"].Text = "fly + antifling"
Converted["_TextButton12"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton12"].TextScaled = true
Converted["_TextButton12"].TextSize = 14
Converted["_TextButton12"].TextWrapped = true
Converted["_TextButton12"].BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Converted["_TextButton12"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton12"].BorderSizePixel = 0
Converted["_TextButton12"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton12"].Parent = Converted["_2"]

Converted["_TextButton13"].Font = Enum.Font.Cartoon
Converted["_TextButton13"].Text = "low gravity"
Converted["_TextButton13"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton13"].TextScaled = true
Converted["_TextButton13"].TextSize = 14
Converted["_TextButton13"].TextWrapped = true
Converted["_TextButton13"].BackgroundColor3 = Color3.fromRGB(0, 0, 255)
Converted["_TextButton13"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton13"].BorderSizePixel = 0
Converted["_TextButton13"].Size = UDim2.new(0, 48, 0, 22)
Converted["_TextButton13"].Parent = Converted["_2"]

Converted["_TextButton14"].Font = Enum.Font.Cartoon
Converted["_TextButton14"].Text = "die"
Converted["_TextButton14"].TextColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton14"].TextScaled = true
Converted["_TextButton14"].TextSize = 14
Converted["_TextButton14"].TextWrapped = true
Converted["_TextButton14"].BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Converted["_TextButton14"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextButton14"].BorderSizePixel = 0
Converted["_TextButton14"].Size = UDim2.new(0, 200, 0, 50)
Converted["_TextButton14"].Parent = Converted["_2"]

Converted["_TextLabel1"].Font = Enum.Font.Cartoon
Converted["_TextLabel1"].Text = "Freecam does not work on mobile."
Converted["_TextLabel1"].TextColor3 = Color3.fromRGB(255, 0, 0)
Converted["_TextLabel1"].TextScaled = true
Converted["_TextLabel1"].TextSize = 14
Converted["_TextLabel1"].TextWrapped = true
Converted["_TextLabel1"].BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Converted["_TextLabel1"].BackgroundTransparency = 1
Converted["_TextLabel1"].BorderColor3 = Color3.fromRGB(0, 0, 0)
Converted["_TextLabel1"].BorderSizePixel = 0
Converted["_TextLabel1"].Position = UDim2.new(0.404715121, 0, 0, 0)
Converted["_TextLabel1"].Size = UDim2.new(0, 303, 0, 37)
Converted["_TextLabel1"].Parent = Converted["_1"]

-- Fake Module Scripts:

local fake_module_scripts = {}


-- Fake Local Scripts:

local function GRSGQS_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	button.MouseButton1Click:Connect(function()
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local head = character:FindFirstChild("Head")
		local rootPart = character:FindFirstChild("HumanoidRootPart")
	
		if humanoid and head and rootPart then
			-- 1. Make the player sit
			humanoid.Sit = true
	
			-- 2. Add Fire to the head (if not already there)
			if not head:FindFirstChild("HeadFire") then
				local fire = Instance.new("Fire")
				fire.Name = "HeadFire"
				fire.Size = 4
				fire.Heat = 15
				fire.Parent = head
			end
	
			-- 3. Make the player spin
			-- We use a connection to spin the HumanoidRootPart for 3 seconds
			local spinConnection
			local startTime = tick()
			local spinDuration = 900 -- Duration in seconds
	
			if not rootPart:FindFirstChild("IsSpinning") then
				local tag = Instance.new("BoolValue")
				tag.Name = "IsSpinning"
				tag.Parent = rootPart
	
				spinConnection = game:GetService("RunService").RenderStepped:Connect(function(dt)
					if tick() - startTime < spinDuration then
						-- Rotate around the Y axis
						rootPart.CFrame = rootPart.CFrame * CFrame.Angles(0, math.rad(108000 * dt * 3), 0) -- 3 full rotations per second
					else
						spinConnection:Disconnect()
						tag:Destroy()
					end
				end)
			end
		end
	end)
end
local function OJDFJ_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton1"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local runService = game:GetService("RunService")
	local userInputService = game:GetService("UserInputService")
	
	local flying = false
	local flyConnection = nil
	local bodyVelocity = nil
	local bodyGyro = nil
	
	-- Track pressed keys manually for smooth flight control
	local keysDown = {
		W = false,
		A = false,
		S = false,
		D = false,
		Space = false,
		LeftControl = false
	}
	
	userInputService.InputBegan:Connect(function(input, gameProcessed)
		if input.KeyCode == Enum.KeyCode.W then keysDown.W = true end
		if input.KeyCode == Enum.KeyCode.A then keysDown.A = true end
		if input.KeyCode == Enum.KeyCode.S then keysDown.S = true end
		if input.KeyCode == Enum.KeyCode.D then keysDown.D = true end
		if input.KeyCode == Enum.KeyCode.Space then keysDown.Space = true end
		if input.KeyCode == Enum.KeyCode.LeftControl then keysDown.LeftControl = true end
	end)
	
	userInputService.InputEnded:Connect(function(input)
		if input.KeyCode == Enum.KeyCode.W then keysDown.W = false end
		if input.KeyCode == Enum.KeyCode.A then keysDown.A = false end
		if input.KeyCode == Enum.KeyCode.S then keysDown.S = false end
		if input.KeyCode == Enum.KeyCode.D then keysDown.D = false end
		if input.KeyCode == Enum.KeyCode.Space then keysDown.Space = false end
		if input.KeyCode == Enum.KeyCode.LeftControl then keysDown.LeftControl = false end
	end)
	
	button.MouseButton1Click:Connect(function()
		flying = not flying
	
		local character = player.Character
		if not character then return end
	
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local camera = workspace.CurrentCamera
	
		if flying then
			button.Text = "Unfly"
			humanoid.PlatformStand = true
	
			-- Create BodyVelocity
			bodyVelocity = Instance.new("BodyVelocity")
			bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
			bodyVelocity.Velocity = Vector3.new(0, 0, 0)
			bodyVelocity.Parent = rootPart
	
			-- Create BodyGyro
			bodyGyro = Instance.new("BodyGyro")
			bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
			bodyGyro.CFrame = rootPart.CFrame
			bodyGyro.Parent = rootPart
	
			flyConnection = runService.RenderStepped:Connect(function()
				if not character or not rootPart or not humanoid or humanoid.Health <= 0 then
					flying = false
					if bodyVelocity then bodyVelocity:Destroy() end
					if bodyGyro then bodyGyro:Destroy() end
					if flyConnection then flyConnection:Disconnect() end
					button.Text = "Fly"
					return
				end
	
				local camCFrame = camera.CFrame
				local speed = 60 -- Flight speed
				local moveVector = Vector3.new(0, 0, 0)
	
				-- Calculate movement based on pressed keys
				if keysDown.W then moveVector = moveVector + camCFrame.LookVector end
				if keysDown.S then moveVector = moveVector - camCFrame.LookVector end
				if keysDown.D then moveVector = moveVector + camCFrame.RightVector end
				if keysDown.A then moveVector = moveVector - camCFrame.RightVector end
				if keysDown.Space then moveVector = moveVector + Vector3.new(0, 1, 0) end
				if keysDown.LeftControl then moveVector = moveVector - Vector3.new(0, 1, 0) end
	
				bodyVelocity.Velocity = moveVector * speed
				bodyGyro.CFrame = camCFrame
			end)
		else
			button.Text = "Fly"
			if humanoid then
				humanoid.PlatformStand = false
			end
	
			if bodyVelocity then bodyVelocity:Destroy() end
			if bodyGyro then bodyGyro:Destroy() end
			if flyConnection then flyConnection:Disconnect() end
		end
	end)
end
local function TXRK_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton2"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local runService = game:GetService("RunService")
	
	local noclipping = false
	local noclipConnection = nil
	
	button.MouseButton1Click:Connect(function()
		noclipping = not noclipping
	
		local character = player.Character
		if not character then return end
	
		if noclipping then
			button.Text = "Noclip: ON"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Turns green when active
	
			-- Loop runs every frame to disable collisions on all character parts
			noclipConnection = runService.Stepped:Connect(function()
				if not player.Character then return end
	
				for _, part in ipairs(player.Character:GetDescendants()) do
					if part:IsA("BasePart") then
						part.CanCollide = false
					end
				end
			end)
		else
			button.Text = "Noclip: on"
			button.BackgroundColor3 = Color3.fromRGB(200, 0, 0) -- Turns red when inactive
	
			-- Disconnect the noclip loop
			if noclipConnection then
				noclipConnection:Disconnect()
				noclipConnection = nil
			end
	
			-- Re-enable collisions for the character parts when turning off
			if player.Character then
				for _, part in ipairs(player.Character:GetDescendants()) do
					if part:IsA("BasePart") then
						-- Keeps the HumanoidRootPart and standard parts collidable again
						part.CanCollide = true
					end
				end
			end
		end
	end)
end
local function WOZHEF_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.FreecamButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_FreecamButton"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local runService = game:GetService("RunService")
	local userInputService = game:GetService("UserInputService")
	local camera = workspace.CurrentCamera
	
	local freecamActive = false
	local freecamConnection = nil
	
	local camPosition = Vector3.new(0, 0, 0)
	local angles = Vector2.new(0, 0)
	local keysDown = { W = false, A = false, S = false, D = false, E = false, Q = false }
	
	-- Track movement keys
	userInputService.InputBegan:Connect(function(input, gameProcessed)
		if not freecamActive then return end
		if input.KeyCode == Enum.KeyCode.W then keysDown.W = true end
		if input.KeyCode == Enum.KeyCode.A then keysDown.A = true end
		if input.KeyCode == Enum.KeyCode.S then keysDown.S = true end
		if input.KeyCode == Enum.KeyCode.D then keysDown.D = true end
		if input.KeyCode == Enum.KeyCode.E then keysDown.E = true end
		if input.KeyCode == Enum.KeyCode.Q then keysDown.Q = true end
	end)
	
	userInputService.InputEnded:Connect(function(input)
		if input.KeyCode == Enum.KeyCode.W then keysDown.W = false end
		if input.KeyCode == Enum.KeyCode.A then keysDown.A = false end
		if input.KeyCode == Enum.KeyCode.S then keysDown.S = false end
		if input.KeyCode == Enum.KeyCode.D then keysDown.D = false end
		if input.KeyCode == Enum.KeyCode.E then keysDown.E = false end
		if input.KeyCode == Enum.KeyCode.Q then keysDown.Q = false end
	end)
	
	-- Directly update camera angles whenever the mouse moves
	userInputService.InputChanged:Connect(function(input)
		if freecamActive and input.UserInputType == Enum.UserInputType.MouseMovement then
			local delta = input.Delta
			angles = angles - Vector2.new(delta.X * 0.003, delta.Y * 0.003)
			angles = Vector2.new(angles.X, math.clamp(angles.Y, -math.pi/2, math.pi/2))
		end
	end)
	
	button.MouseButton1Click:Connect(function()
		freecamActive = not freecamActive
	
		local character = player.Character
		local humanoid = character and character:FindFirstChildOfClass("Humanoid")
		local rootPart = character and character:FindFirstChild("HumanoidRootPart")
	
		if freecamActive then
			button.Text = "Freecam: on"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
	
			-- Freeze your character in place
			if humanoid then humanoid.PlatformStand = true end
			if rootPart then rootPart.Anchored = true end
	
			-- Use LockCurrentPosition so the mouse delta tracks properly while the cursor remains on screen
			userInputService.MouseBehavior = Enum.MouseBehavior.LockCurrentPosition
	
			-- Initialize camera position and angles from current view
			camPosition = camera.CFrame.Position
			local lookVec = camera.CFrame.LookVector
			angles = Vector2.new(math.atan2(-lookVec.X, -lookVec.Z), math.asin(lookVec.Y))
	
			camera.CameraType = Enum.CameraType.Scriptable
	
			freecamConnection = runService.RenderStepped:Connect(function(dt)
				local speed = 60
				local moveVector = Vector3.new(0, 0, 0)
	
				local yaw = CFrame.Angles(0, angles.X, 0)
				local pitch = CFrame.Angles(angles.Y, 0, 0)
				local finalRotation = yaw * pitch
	
				local lookVector = finalRotation.LookVector
				local rightVector = finalRotation.RightVector
	
				if keysDown.W then moveVector = moveVector + lookVector end
				if keysDown.S then moveVector = moveVector - lookVector end
				if keysDown.D then moveVector = moveVector + rightVector end
				if keysDown.A then moveVector = moveVector - rightVector end
				if keysDown.E then moveVector = moveVector + Vector3.new(0, 1, 0) end
				if keysDown.Q then moveVector = moveVector - Vector3.new(0, 1, 0) end
	
				camPosition = camPosition + (moveVector * speed * dt)
				camera.CFrame = CFrame.new(camPosition) * finalRotation
			end)
		else
			button.Text = "Freecam"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	
			if freecamConnection then
				freecamConnection:Disconnect()
				freecamConnection = nil
			end
	
			userInputService.MouseBehavior = Enum.MouseBehavior.Default
	
			-- Unfreeze character
			if rootPart then rootPart.Anchored = false end
			if humanoid then humanoid.PlatformStand = false end
	
			-- Restore normal camera
			camera.CameraType = Enum.CameraType.Custom
			if character and rootPart then
				camera.CameraSubject = humanoid
			end
		end
	end)
end
local function PMVIHFX_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton3"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local mouse = player:GetMouse()
	
	local tpActive = false
	local mouseConnection = nil
	
	button.MouseButton1Click:Connect(function()
		tpActive = not tpActive
	
		if tpActive then
			button.Text = "ClickTP: ON"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Turns green when active
	
			-- Listen for when the player clicks anywhere in the 3D world while active
			mouseConnection = mouse.Button1Down:Connect(function()
				-- Check if the player clicked a valid 3D point in the world
				if mouse.Target then
					local character = player.Character
					if not character then return end
	
					local rootPart = character:FindFirstChild("HumanoidRootPart")
					if not rootPart then return end
	
					-- Get the exact 3D position where the mouse clicked, plus a little height so you don't clip into the floor
					local targetPos = mouse.Hit.Position + Vector3.new(0, 3, 0)
	
					-- Teleport the character instantly
					rootPart.CFrame = CFrame.new(targetPos)
				end
			end)
		else
			button.Text = "ClickTP"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Disconnect the click event when turned off
			if mouseConnection then
				mouseConnection:Disconnect()
				mouseConnection = nil
			end
		end
	end)
end
local function VACBY_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton4"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	button.MouseButton1Click:Connect(function()
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		-- Explicitly set JumpPower mode and apply the value
		humanoid.UseJumpPower = true
		humanoid.JumpPower = 135
	end)
end
local function TPGUG_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton5"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	button.MouseButton1Click:Connect(function()
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		humanoid.WalkSpeed = 70
	end)
end
local function FEYONYW_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton6"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	local flingActive = false
	local touchConnections = {}
	
	-- Function to apply the fling force when you touch another player's part
	local function onPartTouched(hitPart, myRootPart)
		if not flingActive then return end
	
		-- Check if we hit another player's character (not our own)
		local hitCharacter = hitPart.Parent
		local hitHumanoid = hitCharacter:FindFirstChildOfClass("Humanoid")
	
		if hitHumanoid and hitCharacter ~= player.Character then
			local hitRoot = hitCharacter:FindFirstChild("HumanoidRootPart")
			if hitRoot then
				-- Send them flying upward and outward rapidly
				hitRoot.Velocity = (hitRoot.Position - myRootPart.Position).Unit * 300 + Vector3.new(0, 350, 0)
	
				-- Optional: Add a spinning force (BodyAngularVelocity) to make them tumble wildly
				local spin = Instance.new("BodyAngularVelocity")
				spin.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
				spin.AngularVelocity = Vector3.new(math.random(-50, 50), math.random(500, 1000), math.random(-50, 50))
				spin.Parent = hitRoot
	
				-- Remove the spin after 0.5 seconds so they eventually recover
				task.delay(0.5, function()
					if spin then spin:Destroy() end
				end)
			end
		end
	end
	
	-- Connect touch events to all parts of your character
	local function setupCharacter(character)
		-- Clear old connections
		for _, conn in ipairs(touchConnections) do
			conn:Disconnect()
		end
		touchConnections = {}
	
		local rootPart = character:WaitForChild("HumanoidRootPart", 5)
		if not rootPart then return end
	
		for _, part in ipairs(character:GetDescendants()) do
			if part:IsA("BasePart") then
				local conn = part.Touched:Connect(function(hit)
					onPartTouched(hit, rootPart)
				end)
				table.insert(touchConnections, conn)
			end
		end
	end
	
	-- Listen when the button is clicked to toggle Fling mode on/off
	button.MouseButton1Click:Connect(function()
		flingActive = not flingActive
	
		if flingActive then
			button.Text = "Fling: ON"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			if player.Character then
				setupCharacter(player.Character)
			end
		else
			button.Text = "Fling: OFF"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Disconnect touch triggers
			for _, conn in ipairs(touchConnections) do
				conn:Disconnect()
			end
			touchConnections = {}
		end
	end)
	
	-- Re-setup touch connections if your character dies and respawns
	player.CharacterAdded:Connect(function(newChar)
		if flingActive then
			setupCharacter(newChar)
		end
	end)
	
end
local function DPOYRVS_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton7"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local userInputService = game:GetService("UserInputService")
	
	local infJumpActive = false
	local jumpConnection = nil
	
	-- Listen for the jump request (whenever the player presses Space or the jump button)
	jumpConnection = userInputService.JumpRequest:Connect(function()
		if not infJumpActive then return end
	
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local rootPart = character:FindFirstChild("HumanoidRootPart")
	
		if humanoid and rootPart then
			-- Force the humanoid to jump and give a small upward boost
			humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
			rootPart.Velocity = Vector3.new(rootPart.Velocity.X, 50, rootPart.Velocity.Z)
		end
	end)
	
	-- Toggle Infinite Jump on/off when clicking the button
	button.MouseButton1Click:Connect(function()
		infJumpActive = not infJumpActive
	
		if infJumpActive then
			button.Text = "InfJump: ON"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Turns green when active
		else
			button.Text = "InfJump: OFF"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
		end
	end)
	
end
local function AMUWA_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.Button.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_Button"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local mouse = player:GetMouse()
	local runService = game:GetService("RunService")
	
	local dragActive = false
	local dragConnection = nil
	
	button.MouseButton1Click:Connect(function()
		dragActive = not dragActive
	
		if dragActive then
			button.Text = "Drag: ON"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			local character = player.Character
			if not character then return end
	
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			local rootPart = character:FindFirstChild("HumanoidRootPart")
			if not rootPart then return end
	
			-- Disable physics/gravity so you float smoothly where you drag
			if humanoid then humanoid.PlatformStand = true end
			rootPart.Anchored = true
	
			-- Continuously update your root part's position to follow your mouse hit point in the world
			dragConnection = runService.RenderStepped:Connect(function()
				if mouse.Target then
					-- Position your character right where your mouse points, plus a tiny offset so you don't glitch into parts
					local targetPos = mouse.Hit.Position + Vector3.new(0, 3, 0)
					rootPart.CFrame = CFrame.new(targetPos)
				end
			end)
		else
			button.Text = "Drag: OFF"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Stop dragging loop
			if dragConnection then
				dragConnection:Disconnect()
				dragConnection = nil
			end
	
			local character = player.Character
			if character then
				local humanoid = character:FindFirstChildOfClass("Humanoid")
				local rootPart = character:FindFirstChild("HumanoidRootPart")
	
				-- Re-enable normal physics and gravity
				if rootPart then rootPart.Anchored = false end
				if humanoid then humanoid.PlatformStand = false end
			end
		end
	end)
	
end
local function OWRKCYQ_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton8"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	local spinningActive = false
	local spinBodyVel = nil
	
	button.MouseButton1Click:Connect(function()
		spinningActive = not spinningActive
	
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		if not humanoid or not rootPart then return end
	
		if spinningActive then
			button.Text = "wheel: on"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			-- Force the character to sit
			humanoid.Sit = true
	
			-- Create a BodyAngularVelocity to spin on the X-axis
			spinBodyVel = Instance.new("BodyAngularVelocity")
			spinBodyVel.Name = "XAxisSpin"
			spinBodyVel.MaxTorque = Vector3.new(math.huge, 0, 0) -- Lock torque strictly to the X-axis (pitch/flip)
			spinBodyVel.AngularVelocity = Vector3.new(20, 0, 0)   -- Adjust speed (positive or negative for direction)
			spinBodyVel.Parent = rootPart
		else
			button.Text = "wheel"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Remove the spin physics force
			if spinBodyVel then
				spinBodyVel:Destroy()
				spinBodyVel = nil
			end
	
			-- Stand back up
			humanoid.Sit = false
		end
	end)
	
	-- Clean up if character dies/respawns
	player.CharacterAdded:Connect(function()
		spinningActive = false
		button.Text = "wheel"
		button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	end)
	
end
local function TKPTSIH_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton9"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	local isFrozen = false
	local frozenVelocity = Vector3.new(0, 0, 0)
	local frozenCFrame = CFrame.new()
	
	button.MouseButton1Click:Connect(function()
		isFrozen = not isFrozen
	
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		local animator = humanoid and humanoid:FindFirstChildOfClass("Animator")
	
		if not humanoid or not rootPart then return end
	
		if isFrozen then
			button.Text = "freeze: on"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			-- Save current position so they don't shift
			frozenCFrame = rootPart.CFrame
	
			-- Anchor body and disable physics/controls
			rootPart.Anchored = true
			humanoid.PlatformStand = true
			humanoid.WalkSpeed = 0
			humanoid.JumpPower = 0
	
			-- Pause all currently playing animations
			if animator then
				for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
					track:AdjustSpeed(0)
				end
			end
		else
			button.Text = "freeze"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Unfreeze body and restore controls/speeds
			rootPart.Anchored = false
			humanoid.PlatformStand = false
			humanoid.WalkSpeed = 16   -- Default Roblox walkspeed (adjust if your game uses a different speed)
			humanoid.JumpPower = 50   -- Default Roblox jump power (or change back to 135 if using your jump button)
	
			-- Resume animations
			if animator then
				for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
					track:AdjustSpeed(1)
				end
			end
		end
	end)
	
	-- Unfreeze automatically if the player dies/respawns
	player.CharacterAdded:Connect(function()
		isFrozen = false
		button.Text = "Freeze: OFF"
		button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	end)
	
end
local function SJYNVA_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton10"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	local godModeActive = false
	local healthConnection = nil
	
	button.MouseButton1Click:Connect(function()
		godModeActive = not godModeActive
	
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		if godModeActive then
			button.Text = "GodMode: on"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			-- Lock health to max immediately and whenever it tries to change
			humanoid.Health = humanoid.MaxHealth
	
			healthConnection = humanoid.HealthChanged:Connect(function(health)
				if godModeActive and health < humanoid.MaxHealth then
					humanoid.Health = humanoid.MaxHealth
				end
			end)
		else
			button.Text = "GodMode: off"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Disconnect the health lock
			if healthConnection then
				healthConnection:Disconnect()
				healthConnection = nil
			end
		end
	end)
	
	-- Reset GodMode if you die and respawn
	player.CharacterAdded:Connect(function(newChar)
		godModeActive = false
		button.Text = "GodMode: OFF"
		button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		if healthConnection then
			healthConnection:Disconnect()
			healthConnection = nil
		end
	end)
	
end
local function OMRU_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton11"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local playersService = game:GetService("Players")
	
	button.MouseButton1Click:Connect(function()
		local character = player.Character
		if not character then return end
	
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		if not rootPart then return end
	
		local nearestPlayer = nil
		local shortestDistance = math.huge
	
		-- Loop through all players in the server to find the closest one
		for _, otherPlayer in ipairs(playersService:GetPlayers()) do
			-- Skip yourself and players without a character/root part
			if otherPlayer ~= player and otherPlayer.Character then
				local otherRoot = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
				local otherHumanoid = otherPlayer.Character:FindFirstChildOfClass("Humanoid")
	
				if otherRoot and otherHumanoid and otherHumanoid.Health > 0 then
					-- Calculate distance between you and the other player
					local distance = (rootPart.Position - otherRoot.Position).Magnitude
	
					if distance < shortestDistance then
						shortestDistance = distance
						nearestPlayer = otherPlayer
					end
				end
			end
		end
	
		-- If a valid nearest player was found, teleport to them
		if nearestPlayer and nearestPlayer.Character then
			local targetRoot = nearestPlayer.Character:FindFirstChild("HumanoidRootPart")
			if targetRoot then
				-- Teleport slightly behind or above them so you don't glitch inside their body
				rootPart.CFrame = targetRoot.CFrame + Vector3.new(0, 3, 3)
				print("Teleported to nearest player: " .. nearestPlayer.Name)
			end
		else
			print("No other active players found on the server!")
		end
	end)
	
end
local function IMTPTV_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.slide.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_slide"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	local slideActive = false
	
	button.MouseButton1Click:Connect(function()
		slideActive = not slideActive
	
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
	
		if slideActive then
			button.Text = "slide: on"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			-- Lower HipHeight to 0 to make your character crouch/slide low to the ground
			humanoid.HipHeight = 0
		else
			button.Text = "slide"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Restore default HipHeight (usually around 2 for R15 characters)
			humanoid.HipHeight = 2
		end
	end)
	
	-- Reset slide if you die and respawn
	player.CharacterAdded:Connect(function(newChar)
		slideActive = false
		button.Text = "Slide: OFF"
		button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	end)
end
local function YBIWH_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton12"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	local runService = game:GetService("RunService")
	local userInputService = game:GetService("UserInputService")
	local camera = workspace.CurrentCamera
	
	local airSwimActive = false
	local swimConnection = nil
	local keysDown = { W = false, A = false, S = false, D = false, E = false, Q = false }
	
	userInputService.InputBegan:Connect(function(input, gameProcessed)
		if not airSwimActive then return end
		if input.KeyCode == Enum.KeyCode.W then keysDown.W = true end
		if input.KeyCode == Enum.KeyCode.A then keysDown.A = true end
		if input.KeyCode == Enum.KeyCode.S then keysDown.S = true end
		if input.KeyCode == Enum.KeyCode.D then keysDown.D = true end
		if input.KeyCode == Enum.KeyCode.E then keysDown.E = true end -- Up
		if input.KeyCode == Enum.KeyCode.Q then keysDown.Q = true end -- Down
	end)
	
	userInputService.InputEnded:Connect(function(input)
		if input.KeyCode == Enum.KeyCode.W then keysDown.W = false end
		if input.KeyCode == Enum.KeyCode.A then keysDown.A = false end
		if input.KeyCode == Enum.KeyCode.S then keysDown.S = false end
		if input.KeyCode == Enum.KeyCode.D then keysDown.D = false end
		if input.KeyCode == Enum.KeyCode.E then keysDown.E = false end
		if input.KeyCode == Enum.KeyCode.Q then keysDown.Q = false end
	end)
	
	button.MouseButton1Click:Connect(function()
		airSwimActive = not airSwimActive
	
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		if not humanoid or not rootPart then return end
	
		if airSwimActive then
			button.Text = "fly + antifling"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
	
			-- Anchor root part so gravity cannot pull you down at all
			rootPart.Anchored = true
			humanoid.PlatformStand = true
	
			local currentPos = rootPart.Position
	
			swimConnection = runService.RenderStepped:Connect(function(dt)
				local moveDir = Vector3.new(0, 0, 0)
				local camCFrame = camera.CFrame
	
				if keysDown.W then moveDir = moveDir + camCFrame.LookVector end
				if keysDown.S then moveDir = moveDir - camCFrame.LookVector end
				if keysDown.D then moveDir = moveDir + camCFrame.RightVector end
				if keysDown.A then moveDir = moveDir - camCFrame.RightVector end
				if keysDown.E then moveDir = moveDir + Vector3.new(0, 1, 0) end
				if keysDown.Q then moveDir = moveDir - Vector3.new(0, 1, 0) end
	
				if moveDir.Magnitude > 0 then
					moveDir = moveDir.Unit
				end
	
				-- Smoothly update position manually
				currentPos = currentPos + (moveDir * 40 * dt)
				rootPart.CFrame = CFrame.new(currentPos, currentPos + camCFrame.LookVector)
			end)
		else
			button.Text = "fly + antifling"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	
			if swimConnection then
				swimConnection:Disconnect()
				swimConnection = nil
			end
	
			rootPart.Anchored = false
			humanoid.PlatformStand = false
			for k, _ in pairs(keysDown) do keysDown[k] = false end
		end
	end)
	
	player.CharacterAdded:Connect(function()
		airSwimActive = false
		button.Text = "fly + antifling"
		button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		if swimConnection then
			swimConnection:Disconnect()
			swimConnection = nil
		end
		for k, _ in pairs(keysDown) do keysDown[k] = false end
	end)
end
local function VKZO_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton13"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	local lowGravActive = false
	local bodyForce = nil
	
	button.MouseButton1Click:Connect(function()
		lowGravActive = not lowGravActive
	
		local character = player.Character
		if not character then return end
	
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		if not rootPart then return end
	
		if lowGravActive then
			button.Text = "low gravity: on"
			button.BackgroundColor3 = Color3.fromRGB(0, 200, 0) -- Green when active
	
			-- Create an upward VectorForce to counteract most of Roblox's gravity (Workspace.Gravity is 196.2 by default)
			-- We attach it to a VectorForce or BodyForce
			bodyForce = Instance.new("BodyForce")
			bodyForce.Name = "LowGravityForce"
	
			-- Mass * Gravity = Total weight. We want to cancel out about 80% of it so you float gently.
			local characterMass = 0
			for _, part in ipairs(character:GetDescendants()) do
				if part:IsA("BasePart") then
					characterMass = characterMass + part:GetMass()
				end
			end
	
			-- Apply an upward force equal to 80% of your total weight
			bodyForce.Force = Vector3.new(0, characterMass * workspace.Gravity * 0.8, 0)
			bodyForce.Parent = rootPart
		else
			button.Text = "low gravity"
			button.BackgroundColor3 = Color3.fromRGB(255, 255, 255) -- Reset color
	
			-- Remove the custom gravity force
			if bodyForce then
				bodyForce:Destroy()
				bodyForce = nil
			end
		end
	end)
	
	-- Reset LowGrav if you die and respawn
	player.CharacterAdded:Connect(function(newChar)
		lowGravActive = false
		button.Text = "LowGrav: OFF"
		button.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		if bodyForce then
			bodyForce:Destroy()
			bodyForce = nil
		end
	end)
	
end
local function HTFPM_fake_script() -- Fake Script: StarterGui.Admin Panel.1.2.TextButton.LocalScript
    local script = Instance.new("LocalScript")
    script.Name = "LocalScript"
    script.Parent = Converted["_TextButton14"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	local button = script.Parent
	local player = game.Players.LocalPlayer
	
	button.MouseButton1Click:Connect(function()
		local character = player.Character
		if not character then return end
	
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			-- Instantly set health to 0 to trigger death/respawn
			humanoid.Health = 0
		end
	end)
	
end
local function ISRPRV_fake_script() -- Fake Script: StarterGui.Admin Panel.1.MoveFrame
    local script = Instance.new("LocalScript")
    script.Name = "MoveFrame"
    script.Parent = Converted["_1"]
    local req = require
    local require = function(obj)
        local fake = fake_module_scripts[obj]
        if fake then
            return fake()
        end
        return req(obj)
    end

	-- @maeesv || Поместите скрипт в ваш фрейм.
	
	script.Parent.Active = true    -- Включает галочку Active у фрейма
	script.Parent.Draggable = true   -- Включает саму функцию перетаскивания
	
	-- .gg/BurqcY6HqV
end

coroutine.wrap(GRSGQS_fake_script)()
coroutine.wrap(OJDFJ_fake_script)()
coroutine.wrap(TXRK_fake_script)()
coroutine.wrap(WOZHEF_fake_script)()
coroutine.wrap(PMVIHFX_fake_script)()
coroutine.wrap(VACBY_fake_script)()
coroutine.wrap(TPGUG_fake_script)()
coroutine.wrap(FEYONYW_fake_script)()
coroutine.wrap(DPOYRVS_fake_script)()
coroutine.wrap(AMUWA_fake_script)()
coroutine.wrap(OWRKCYQ_fake_script)()
coroutine.wrap(TKPTSIH_fake_script)()
coroutine.wrap(SJYNVA_fake_script)()
coroutine.wrap(OMRU_fake_script)()
coroutine.wrap(IMTPTV_fake_script)()
coroutine.wrap(YBIWH_fake_script)()
coroutine.wrap(VKZO_fake_script)()
coroutine.wrap(HTFPM_fake_script)()
coroutine.wrap(ISRPRV_fake_script)()